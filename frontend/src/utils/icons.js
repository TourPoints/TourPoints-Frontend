import { createIcons, Search, CircleUserRound, Heart, Star } from "lucide";

export const loadIcons = () => {
  createIcons({
    icons: {
      Search,
      CircleUserRound,
      Heart,
      Star,
    },
  });
};
